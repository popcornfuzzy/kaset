# Build artifacts
.build/
DerivedData/
*.xcuserstate
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM
*.profraw

# Xcode (auto-generated from Package.swift)
*.xcodeproj/
!KasetUITests.xcodeproj/
*.xcworkspace/
*.xcuserdata/
xcuserdata/

# Swift Package Manager
/.swiftpm
Packages/
Package.pins
.netrc
# Package.resolved is deliberately tracked: this is an app, not a library, and
# CI must resolve the same Sparkle version it ships. A floating Sparkle also
# floats the generate_appcast/sign_update behaviour used to sign the appcast.

# CocoaPods (if ever used)
Pods/

# Carthage (if ever used)
Carthage/Build
Carthage/Checkouts

# fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

# OS files
.DS_Store
.AppleDouble
.LSOverride
._*

# Thumbnails
Thumbs.db

# IDE
.idea/
*.swp
*.swo
*~

# Environment and secrets
.env
.env.local
*.pem

TEMP_*
build/
.releases/
releases/
/.freebuff
.vscode
